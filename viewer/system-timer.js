/* Change Weather & Time ====================================================================================== */

function SystemTimer(hours = 0, minutes = 0) {
    

    text(`${fb.number.prefixZero(hours)}:${fb.number.prefixZero(minutes)} ${half}`);
}

